package day12;

public interface Compute {
	void add(); //Inside the interface method is by default public & abstract(write abstract or not doesn't matter but it can run error free)
	abstract void sub();
}
